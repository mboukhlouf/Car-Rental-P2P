@model IEnumerable<Api.Models.Annonce>

@{
    ViewData["Title"] = "Index2";
}

            <!-- Default box -->
              
                    @foreach (var item in Model)
                    {
                         @await Html.PartialAsync("DisplayAnnonce",item)

                    }
                   
